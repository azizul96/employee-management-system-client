
const DashboardTitle = ({heading}) => {
    return (
        <div className="text-end mb-5">
            <h3 className="text-3xl uppercase font-bold text-[#8a50fb] ">{heading}</h3>
        </div>
    );
};

export default DashboardTitle;